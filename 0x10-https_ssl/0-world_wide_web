#!/usr/bin/env bash
# script that will display information about subdomains

domain_info () {
    line=$(dig "$2"."$1" | grep -A1 'ANSWER SECTION:' | tr '\t' '\n' | tail -2 | tr '\n' ' ')
    echo "$2 $line" | awk '{print "The subdomain " $1 " is a " $2 " record and points to " $3}'
}

<<<<<<< HEAD
if [ "$#" == 1 ]
then
    domain_info "$1" "www"
    domain_info "$1" "lb-01"
    domain_info "$1" "web-01"
    domain_info "$1" "web-02"
elif [ "$#" == 2 ]
then
    domain_info "$1" "$2"
=======
if [ -z "$2" ]; then
   for subs in "www" "35.175.65.147" "54.144.199.197" "34.207.61.128"
   do
       print_rec "$1" "$subs"
   done
else
    print_rec "$1" "$2"
>>>>>>> 063069d8aaece5f21bb5a52f86bdeb876062637e
fi
